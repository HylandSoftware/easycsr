spec:
  containers:
  - name: jnlp
    image: hcr.io/jenkins/jnlp-slave:alpine
  - name: gitversion
    image: gittools/gitversion-dotnetcore:linux-4.0.1
    command:
      - cat
    tty: true
  - name: docker
    image: docker:19.03-dind
    securityContext:
      privileged: true
